<h1>Add a new produce!</h1>

<%= form_for @produce do |f| %>

    <div>
        <%= f.label :name %>
        <%= f.text_field :name %>
    </div>
    
    <%= f.submit "Add new produce!" %>

<% end %>

<%= link_to "Back to the list!", produces_path %>
